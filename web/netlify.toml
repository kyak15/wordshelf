# Netlify config for WordVault web (Next.js in monorepo)
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  base = "web"
  command = "npm run build"

# Next.js plugin sets publish dir itself — do not set "Publish directory" in Netlify UI
[[plugins]]
  package = "@netlify/plugin-nextjs"
